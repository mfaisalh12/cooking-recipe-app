import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  btnContainer: {
    alignItems: 'center',
    padding: 8,
  },
  btnIcon: {
    fontSize: 30,
    color: '#FF8303',
  },
});

export default styles;
